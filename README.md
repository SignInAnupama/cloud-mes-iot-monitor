# cloud-mes-iot-monitor
IoT machine monitor dashboard Simulate PLC/sensor data with Python (you already know the data shapes), push it over MQTT to AWS IoT Core or Azure IoT Hub, store in a time-series DB (InfluxDB or AWS Timestream), and visualize with Grafana. 
